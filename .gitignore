# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
*.local

# Note: dist/ is committed on purpose so cPanel never has to build.
# Build locally with `npm run build` before committing.

# Environment
.env
.env.*
!.env.example

# Local database (lives on the server, never committed)
*.sqlite
*.sqlite-shm
*.sqlite-wal

# Uploaded course files (runtime data, lives on the server, backed up via FTP)
storage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
